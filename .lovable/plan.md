

Update the hardcoded impact stats in `src/components/home/ImpactStats.tsx`:

- Funds Raised: $2,500,000 → $40,000 (display as "$40K+")
- Lives Impacted: 50,000 → 1,000 (display as "1K+")  
- Projects Completed: 120 → 90
- Active Volunteers: 3,000 → 20 (display as "20+")

Single file change to the `stats` array at the top of the file.

